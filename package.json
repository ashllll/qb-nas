{
  "name": "qb-nas-hooks",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint": "node scripts/run-python.cjs -m ruff check magnet_harvester tests",
    "test": "node scripts/run-python.cjs -m pytest tests -q",
    "coverage": "node scripts/run-python.cjs -m pytest tests -q --cov",
    "check": "npm run lint && npm run coverage"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2"
  }
}
