{
  "name": "qb-nas-hooks",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint": "./.venv/bin/python -m ruff check magnet_harvester tests",
    "test": "./.venv/bin/python -m pytest tests -q",
    "check": "npm run lint && npm test"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2"
  }
}
